# Claude Code Instructions

Read and follow [`AGENTS.md`](./AGENTS.md) as the canonical repository guide.
It defines the project structure, development commands, coding conventions,
validation requirements, prototype constraints, and browser policy.

Keep repository-wide guidance in `AGENTS.md` to avoid duplicated instructions
drifting out of sync. Add content here only when it applies specifically to
Claude Code and does not belong in the shared contributor guide.
